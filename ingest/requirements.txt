# Core (required to run the ingester)
httpx>=0.27
beautifulsoup4>=4.12
html2text>=2024.2.26
psycopg[binary]>=3.1
python-dotenv>=1.0
anthropic>=0.40

# Optional crawler backends. The ingester auto-detects whichever is available
# and falls back to httpx + html2text when neither is installed.
#   pip install crawl4ai   # then: crawl4ai-setup   (installs Playwright)
#   (Firecrawl needs no package — set FIRECRAWL_API_KEY to use its hosted API)
# crawl4ai>=0.4
